
import { Book, User } from './types';

// Mock data has been removed to support live MERN backend.
// Use the Admin Dashboard to add books and users to the database.
export const INITIAL_BOOKS: Book[] = [];
export const INITIAL_USERS: User[] = [];
